import Register from "../Containers/Home/Register";

const Home = () => {
  return (
    <div className="relative flex justify-center items-center h-screen container">
      <Register />
    </div>
  );
};

export default Home;
